# Integration Tests

Phase 1 integration acceptance is manual until a full CI environment with Docker and RADIUS tooling is available.

Required checks:
- Production install completes.
- Staging install completes.
- Production and staging run on the same server.
- Admin can create NAS/router/AP clients, users, manual balance, and verify sessions/auth logs.
